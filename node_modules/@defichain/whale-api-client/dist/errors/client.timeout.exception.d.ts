/**
 * Local client exception, due to local reason.
 */
export declare class WhaleClientException extends Error {
}
/**
 * Whale client timeout locally.
 */
export declare class WhaleClientTimeoutException extends WhaleClientException {
    readonly timeout: number;
    constructor(timeout: number);
}
